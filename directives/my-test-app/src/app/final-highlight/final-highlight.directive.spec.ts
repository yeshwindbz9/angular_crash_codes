import { FinalHighlightDirective } from './final-highlight.directive';

describe('FinalHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FinalHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
